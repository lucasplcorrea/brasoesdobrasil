name: brasoes-do-brasil

services:
  coletor:
    build:
      context: .
    image: brasoes-do-brasil-worker:local
    user: '${WORKER_UID:-1000}:${WORKER_GID:-1000}'
    restart: on-failure:5
    init: true
    env_file:
      - .env.worker
    volumes:
      - ./data:/app/data
      - ./assets:/app/assets
      - ./docs:/app/docs
    read_only: true
    tmpfs:
      - /tmp:size=256m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    stop_grace_period: 60s
